#include <time.h>
#include <stdlib.h>
#include "programas.h"


int main () {
    srand(time(NULL));
    //programaAleatorio(50);
    //programaMultiplica(NULL, 10, 10);
    programaDivisao(NULL, 69, 10);

    return 0;
}
